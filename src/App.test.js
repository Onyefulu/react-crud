// import { render, screen } from "@testing-library/react";
// import App from "./App";

// test("renders registration box link", () => {
//   render(<App/>);
//   const linkElement = screen.getByText(/registration box/);
//   expect(linkElement).toBeInTheDocument();
// });
